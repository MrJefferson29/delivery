{
  "version": 2,
  "builds": [
    {
      "src": "server.js", // replace with your entry point file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
